# GitLab CI/CD Pipeline Configuration Reference: https://docs.gitlab.com/ee/ci/yaml/

default:
  image: node:latest
  # https://docs.gitlab.com/ee/ci/caching/#caching-nodejs-dependencies
  cache:
    key: ${CI_COMMIT_REF_SLUG}
    paths:
      - node_modules/

# https://docs.gitlab.com/ee/ci/yaml/#stages
stages:
  - build
  - test
  - deploy

.build:
  stage: build
  script:
    - yarn
    - yarn build
  artifacts:
    untracked: true

.deploy:
  stage: deploy
  script:
    - yarn deploy

.staging:
  variables:
    PUBLIC_URL: $STAGING_PUBLIC_URL
    OAUTH2_DOMAIN: $STAGING_OAUTH2_DOMAIN
    OAUTH2_CLIENTID: $STAGING_OAUTH2_CLIENTID
    OAUTH2_REDIRECT_URI: $STAGING_OAUTH2_REDIRECT_URI
    OAUTH2_LOGOUT_URL: $STAGING_OAUTH2_LOGOUT_URL
    OAUTH2_SCOPE: $STAGING_OAUTH2_SCOPE

.production:
  variables:
    PUBLIC_URL: $PRODUCTION_PUBLIC_URL
    OAUTH2_DOMAIN: $PRODUCTION_OAUTH2_DOMAIN
    OAUTH2_CLIENTID: $PRODUCTION_OAUTH2_CLIENTID
    OAUTH2_REDIRECT_URI: $PRODUCTION_OAUTH2_REDIRECT_URI
    OAUTH2_LOGOUT_URL: $PRODUCTION_OAUTH2_LOGOUT_URL
    OAUTH2_SCOPE: $PRODUCTION_OAUTH2_SCOPE

build_staging:
  extends:
    - .build
    - .staging
  except:
    - /^release.*$/

build_production:
  extends:
    - .build
    - .production
  only:
    - /^release.*$/

test:
  stage: test
  script:
    - yarn test
  artifacts:
    untracked: true

lint:
  stage: test
  script:
    - yarn lint
  artifacts:
    untracked: true

deploy_staging:
  extends:
    - .deploy
    - .staging
  # https://docs.gitlab.com/ee/ci/environments.html#defining-environments
  environment:
    name: staging
    url: $STAGING_PUBLIC_URL
  only:
    - master

deploy_production:
  extends:
    - .deploy
    - .production
  # https://docs.gitlab.com/ee/ci/environments.html#defining-environments
  environment:
    name: production
    url: $PRODUCTION_PUBLIC_URL
  only:
    - /^release.*$/
