# Analytics

Powered by [Google Analytics](https://analytics.google.com) and [gatsby-plugin-google-analytics](https://github.com/gatsbyjs/gatsby/tree/master/packages/gatsby-plugin-google-analytics).

The analytics script is only integrated in production builds. Test it with:

```sh
yarn build
yarn serve
```
