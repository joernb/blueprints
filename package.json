{
  "name": "TODO-set-node-package-name",
  "private": true,
  "version": "1.0.0",
  "dependencies": {},
  "devDependencies": {
    "@aws-cdk/aws-apigateway": "^1.18.0",
    "@aws-cdk/aws-lambda": "^1.18.0",
    "@aws-cdk/core": "^1.8.0",
    "@types/node": "^12.7.2",
    "aws-cdk": "^1.8.0",
    "dotenv": "^8.2.0",
    "nodemon": "^1.19.4",
    "prettier": "^1.18.2",
    "ts-node": "^8.3.0",
    "tslint": "^5.18.0",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^3.5.3"
  },
  "scripts": {
    "format": "prettier --write src/**/*.{js,jsx,ts,tsx}",
    "build": "echo \"TODO: Write build script!\" && yarn bundle:deps",
    "test": "echo \"TODO: Write test script!\"",
    "lint": "prettier --check src/**/*.{js,jsx,ts,tsx} && tslint -c tslint.json 'src/**/*.{js,jsx,ts,tsx}'",
    "deploy": "yarn bundle:deps && yarn cdk:bootstrap && yarn cdk:deploy --require-approval never",
    "bundle:deps": "cp package.json dist/package.json && cd dist && yarn install --production",
    "cdk:bootstrap": "NODE_OPTIONS='-r dotenv/config' cdk bootstrap",
    "cdk:deploy": "NODE_OPTIONS='-r dotenv/config' cdk deploy",
    "cdk:diff": "NODE_OPTIONS='-r dotenv/config' cdk diff",
    "cdk:destroy": "NODE_OPTIONS='-r dotenv/config' cdk destroy"
  }
}
