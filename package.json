{
  "name": "TODO-set-node-package-name",
  "private": true,
  "version": "1.0.0",
  "dependencies": {
    "apollo-server": "^2.9.7",
    "graphql": "^14.5.8"
  },
  "devDependencies": {
    "@babel/cli": "^7.6.4",
    "@babel/core": "^7.6.4",
    "@babel/node": "^7.6.3",
    "@babel/preset-env": "^7.6.3",
    "dotenv": "^8.2.0",
    "nodemon": "^1.19.4",
    "prettier": "^1.18.2"
  },
  "scripts": {
    "format": "prettier --write src/**/*.{js,jsx}",
    "start": "nodemon --watch src --exec babel-node --extensions '.js' src/index.js",
    "build": "babel src --out-dir dist --extensions '.js'",
    "serve": "node dist/index.js",
    "test": "echo \"TODO: Write test script!\"",
    "lint": "prettier --check src/**/*.{js,jsx}",
    "deploy": "echo \"TODO: Write deployment script!\""
  }
}
