{
  "name": "TODO-set-node-package-name",
  "private": true,
  "version": "1.0.0",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@babel/cli": "^7.6.4",
    "@babel/core": "^7.6.4",
    "@babel/node": "^7.6.3",
    "@babel/preset-env": "^7.6.3",
    "dotenv": "^8.2.0",
    "nodemon": "^1.19.4",
    "prettier": "^1.18.2"
  },
  "scripts": {
    "start": "NODE_OPTIONS='-r dotenv/config' nodemon --watch src --exec babel-node --extensions '.js' src/serve.js",
    "format": "prettier --write src/**/*.{js,jsx}",
    "build": "babel src --out-dir dist --extensions '.js'",
    "serve": "node dist/serve.js",
    "test": "echo \"TODO: Write test script!\"",
    "lint": "prettier --check src/**/*.{js,jsx}",
    "deploy": "echo \"TODO: Write deployment script!\""
  }
}
