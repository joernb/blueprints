{
  "name": "TODO-set-node-package-name",
  "private": true,
  "version": "1.0.0",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/node": "^12.7.2",
    "dotenv": "^8.2.0",
    "prettier": "^1.18.2",
    "ts-node": "^8.3.0",
    "tslint": "^5.18.0",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^3.5.3"
  },
  "scripts": {
    "start": "NODE_OPTIONS='-r dotenv/config' node src/index.js",
    "format": "prettier --write src/**/*.{js,jsx,ts,tsx}",
    "build": "echo \"TODO: Write build script!\"",
    "test": "echo \"TODO: Write test script!\"",
    "lint": "prettier --check src/**/*.{js,jsx,ts,tsx} && tslint -c tslint.json 'src/**/*.{js,jsx,ts,tsx}'",
    "deploy": "echo \"TODO: Write deployment script!\""
  }
}
